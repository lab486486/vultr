# Public site URL
PUBLIC_SITE_URL=https://vultr.seoul.kr

# Optional local override for Chemicloud affiliate
# (also editable in Decap → 사이트 설정 → chemicloud_url)
PUBLIC_CHEMICLOUD_URL=

# Decap CMS GitHub OAuth
# Local: copy to .env (gitignored) — never commit secrets
# Production: Cloudflare Pages → Settings → Environment variables (Secrets)
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# R2 public base (also in wrangler.toml [vars] and public/admin/config.yml)
# R2_PUBLIC_BASE_URL=https://pub-9b066cc3e4094ff8946656c10cbb9f3d.r2.dev
